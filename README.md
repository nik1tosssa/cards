# 📚 Структура проекта "Карточки"

## Обзор
Мультифункциональная платформа с мини-играми для обучения:
- 🎮 **Соотнеси карточки** - перетаскивание концепций на определения
- 🎡 **Колесо Фортуны** - вращение колеса для выбора элементов
- 📚 **Flashcards** - просмотр карточек (в разработке)
- ⏱️ **Practice** - тренировка с таймером (в разработке)

## Структура файлов

```
cards/
├── 📄 HTML страницы
│   ├── index.html              # 🏠 Главная с выбором активностей
│   ├── matches.html            # 🎮 Соотнеси карточки
│   ├── fortuneWheel.html       # 🎡 Колесо Фортуны
│   ├── flashcards.html         # 📚 Flashcards (заглушка)
│   └── practice.html           # ⏱️ Тренировка (заглушка)
│
├── 🎨 CSS
│   └── styles.css              # Единые стили для всех страниц
│
├── 🔧 JavaScript модули
│   └── js/
│       ├── game.js             # Контроллер matches.html
│       ├── gameState.js        # Состояние игры
│       ├── timer.js            # Таймер
│       ├── cardManager.js      # Управление карточками
│       ├── dragDropHandler.js  # Перетаскивание
│       ├── gameUI.js           # UI и модали
│       ├── fileLoader.js       # Загрузка JSON
│       ├── fortuneWheel.js     # Логика Колеса Фортуны
│       └── activitySelector.js # Навигация
│
├── 📊 JSON данные
│   ├── jsons/
│   │   ├── data.json                   # Карточки для matches
│   │   └── fortuneWheel_example.json   # Слова для Колеса
│   └── example_cards.json              # Пример формата
│
└── 📚 Документация
    ├── 00_START_HERE.md        # 👈 Начните отсюда
    ├── README.md               # Этот файл
    ├── QUICKSTART.md           # Быстрый старт
    ├── ARCHITECTURE.md         # Архитектура
    ├── USAGE.md                # Инструкции
    ├── FORTUNEWHEEL_README.md  # Колесо Фортуны
    ├── CHANGES.md              # История
    └── DIAGRAM.txt             # Диаграммы
```

## 🎮 Модули для Соотнеси карточки

### **game.js** - Главный контроллер
Основной управляющий модуль для matches.html
- `GameController.init()` - инициализация при загрузке страницы
- `GameController.startGame(data)` - запуск/перезапуск игры
- Координирует все остальные модули
- ⬅️ Зависит от: gameState, timer, cardManager, dragDropHandler, gameUI, fileLoader

### **gameState.js** - Состояние игры
Управление состоянием текущей игры
- `GameState.reset()` - очистить состояние
- `GameState.incrementMatched()` - +1 совпадению
- `GameState.incrementErrors()` - +1 ошибке
- `GameState.setCardData(data)` - загрузить карточки

### **timer.js** - Таймер
Отслеживание времени игры
- `GameTimer.start(element)` - начать отсчет
- `GameTimer.stop()` - остановить таймер
- `GameTimer.formatTime(ms)` - форматировать в mm:ss

### **cardManager.js** - Управление карточками
Создание и рендеринг DOM элементов карточек
- `CardManager.renderCards()` - отрисовать карточки на поле
- `CardManager.createCardElement(card)` - создать элемент карточки
- `CardManager.markAsMatched(id)` - отметить совпадение
- Использует Fisher-Yates для перемешивания

### **dragDropHandler.js** - Перетаскивание
Обработка Drag-and-Drop событий
- `DragDropHandler.handleDragStart()` - начало перетаскивания
- `DragDropHandler.handleDrop()` - сброс карточки
- `DragDropHandler.handleSuccessfulMatch()` - правильное совпадение ✅
- `DragDropHandler.handleMismatch()` - неправильное совпадение ❌

### **gameUI.js** - Интерфейс
Управление UI и модальными окнами
- `UIManager.updateScore()` - обновить счет на экране
- `UIManager.showGameStatusModal()` - показать окно с результатами
- `UIManager.showLoadingMessage()` - показать сообщение загрузки
- `UIManager.resetUI()` - сбросить UI к начальному состоянию

### **fileLoader.js** - Загрузка JSON
Загрузка и валидация файлов данных
- `FileLoader.handleFileLoad(event)` - обработать загрузку файла
- `FileLoader.validateCardData(data)` - проверить формат JSON
- Парсит JSON и передает данные GameController

## 🎡 Модули для Колеса Фортуны

### **fortuneWheel.js** - Логика Колеса
Полное управление мини-игрой Колесо Фортуны для fortuneWheel.html
- `FortuneWheelGame.init()` - инициализация при загрузке
- `FortuneWheelGame.loadGameData(data)` - загрузить слова/данные
- `FortuneWheelGame.spin()` - вращение колеса с 2-сек анимацией
- `FortuneWheelGame.showResult()` - показать выбранное слово в модали
- `FortuneWheelGame.removeSelectedWord()` - удалить слово из колеса
- `FortuneWheelGame.closeModal()` - закрыть модальное окно
- Самодостаточный синглтон-объект

## 🌐 Модули навигации

### **activitySelector.js** - Выбор активности
Навигация между мини-играми на главной странице (index.html)
- Управляет выбором активности
- Показывает описания игр
- Перенаправляет на нужный HTML файл
  - `startGame()` - запуск/перезапуск игры
  - `init()` - инициализация при загрузке страницы
  - Связь всех модулей вместе

## Поток данных

```
1. Загрузка страницы (window.load)
   ↓
2. GameController.init()
   ├─ UIManager.init() - инициализация DOM ссылок
   └─ GameController.startGame() - первый запуск
   
3. Во время игры:
   ├─ Пользователь перетаскивает карточку
   ├─ DragDropHandler перехватывает события
   ├─ GameState обновляется
   ├─ UIManager обновляет интерфейс
   └─ GameTimer отслеживает время
   
4. Окончание игры:
   ├─ Проверка GameState.isAllMatched() или GameState.isMaxErrorsReached()
   ├─ GameTimer.stop()
   └─ UIManager.showGameStatusModal()
```

## 🔗 Межфайловые связи

### index.html
- ➡️ Загружает: js/activitySelector.js
- ➡️ Ссылается на: matches.html, fortuneWheel.html, flashcards.html, practice.html
- 🎨 Использует: styles.css

### matches.html
- ➡️ Загружает: js/gameState.js, js/timer.js, js/cardManager.js, js/dragDropHandler.js, js/gameUI.js, js/fileLoader.js, js/game.js
- 🎨 Использует: styles.css
- 📁 Читает: JSON файлы из jsons/ папки

### fortuneWheel.html
- ➡️ Загружает: js/fortuneWheel.js
- 🎨 Использует: styles.css
- 📁 Читает: JSON файлы (массивы строк) из jsons/ папки

### styles.css
- 🎨 Применяется: ко всем HTML страницам
- 📦 Использует: Tailwind CSS (CDN)
- 🎯 Определяет: общие стили, анимации, адаптивность

## 📋 JSON форматы

### Для matches.html (jsons/data.json)
```json
[
  {"id": 1, "type": "concept", "content": "...", "matchId": 2},
  {"id": 2, "type": "definition", "content": "...", "matchId": 1}
]
```

### Для fortuneWheel.html (jsons/fortuneWheel_example.json)
```json
["Слово 1", "Слово 2", "Слово 3"]
```

## ✅ Статус функциональности

| Функция | Статус | Примечание |
|---------|--------|-----------|
| Соотнеси карточки | ✅ Готово | Полная реализация с перетаскиванием |
| Колесо Фортуны | ✅ Готово | Вращение, выбор, удаление слов |
| Flashcards | ⏳ Заглушка | Требует реализации |
| Practice | ⏳ Заглушка | Требует реализации |
| Загрузка JSON | ✅ Готово | Для обеих игр |
| Адаптивный дизайн | ✅ Готово | Tailwind CSS |
| Консистентный UI | ✅ Готово | Единые стили для всех страниц |
[
  { "id": 1, "type": "concept", "content": "HTML", "matchId": 2 },
  { "id": 2, "type": "definition", "content": "Язык разметки...", "matchId": 1 },
  { "id": 3, "type": "concept", "content": "CSS", "matchId": 4 },
  { "id": 4, "type": "definition", "content": "Описывает стиль...", "matchId": 3 }
]
```

**Требования:**
- Массив четной длины
- Каждый элемент имеет: `id`, `type`, `content`, `matchId`
- `type` - "concept" или "definition"
- `matchId` - ID карточки, с которой должно совпасть

## Возможности расширения

Благодаря модульной структуре легко можно:
- Добавить новые типы карточек
- Реализовать сохранение результатов
- Добавить уровни сложности
- Интегрировать API для загрузки данных
- Добавить звуковые эффекты
- Создать мультиплеер

## Миграция

Чтобы использовать новую версию:
1. Замените `matches.html` на `index_new.html` (или переименуйте обратно)
2. Создайте директорию `js/` со всеми модулями
3. Убедитесь, что `styles.css` находится в корне проекта
4. Все остальное работает автоматически

## Отличия новой версии

✅ Модульная архитектура
✅ Легче тестировать отдельные компоненты
✅ Лучше организованный код
✅ Легче добавлять новые функции
✅ Уменьшен размер основного файла
✅ Повышена переиспользуемость кода
