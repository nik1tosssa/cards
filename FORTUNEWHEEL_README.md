# 🎡 Колесо Фортуны - Мини-игра

## Описание

**Колесо Фортуны** - это интерактивная мини-игра, где игрок вращает виртуальное колесо для случайного выбора слова или элемента из загруженного списка.

## Возможности

- 🎲 **Вращение колеса** - плавная 2-секундная анимация с ease-out эффектом
- ✨ **Красивая визуализация** - HTML5 Canvas с цветными сегментами
- 📊 **Загрузка данных** - JSON файлы со списками слов
- 🗑️ **Удаление слов** - удалять выбранные элементы прямо из игры
- 📋 **Модальное окно** - результат в красивом диалоговом окне
- 📱 **Адаптивный дизайн** - работает на всех экранах

## Структура файлов

```
fortuneWheel.html           ← Основная страница игры
js/fortuneWheel.js          ← Логика игры
jsons/fortuneWheel_example.json  ← Пример JSON файла
```

## Как использовать

### 1. Откройте страницу игры
Откройте `fortuneWheel.html` в веб-браузере или нажмите кнопку на главной странице.

### 2. Подготовьте JSON файл
Создайте JSON файл со списком слов:

```json
["Слово 1", "Слово 2", "Слово 3", "Слово 4"]
```

ИЛИ с объектами (для совместимости):

```json
[{"text": "Слово 1"}, {"text": "Слово 2"}]
```

### 3. Загрузите файл
- Нажмите кнопку **"Загрузить JSON-файл"**
- Выберите ваш JSON файл
- Колесо автоматически инициализируется

### 4. Вращайте колесо
- Нажмите **"🎲 Вращать колесо"**
- Ждите 2 секунды - колесо вращается
- Слово выпадет в модальном окне

### 5. Результат
- Видите выбранное слово в красивой модали
- Кнопка **"✕ Закрыть"** - закрыть окно
- Кнопка **"🗑️ Убрать слово"** - удалить слово из колеса

## Формат JSON файла

### Простой формат (рекомендуется)
```json
["Элемент 1", "Элемент 2", "Элемент 3"]
```

### Формат с объектами
```json
[{"text": "Элемент 1"}, {"text": "Элемент 2"}]
```

## Примеры JSON файлов

### Пример 1: Животные
```json
["Кот", "Собака", "Птица", "Рыба", "Слон", "Лев", "Гепард", "Жираф"]
```

### Пример 2: Цвета
```json
["Красный", "Синий", "Зеленый", "Желтый", "Оранжевый", "Фиолетовый"]
```

### Пример 3: Города
```json
["Москва", "Париж", "Лондон", "Токио", "Нью-Йорк", "Дубай", "Берлин"]
```

## Технические детали

### Canvas
- Размер: 400×400px
- Рисуется в js/fortuneWheel.js методом drawWheelRotated()
- Каждый сегмент имеет разный цвет
- Текст отцентрирован в сегменте

### Анимация
- Длительность: 2 секунды (2000ms)
- Функция ease-out: `1 - (1 - progress)³`
- requestAnimationFrame для плавности

### Данные
- Хранятся в `FortuneWheelGame.gameData`
- Формат: массив строк
- Поддерживаются как простые строки, так и объекты {text}

### Модальное окно
- ID: `status-modal`
- Появляется после выбора слова
- Две кнопки: закрыть и удалить
- Закрывается кликом на кнопку или через код

## Управление игрой

### Методы (используются внутренне)

```javascript
// Инициализация
FortuneWheelGame.init()

// Загрузка данных
FortuneWheelGame.loadGameData(data)

// Инициализация колеса (рисование)
FortuneWheelGame.initializeWheel()

// Вращение колеса
FortuneWheelGame.spin()

// Показ результата
FortuneWheelGame.showResult()

// Удаление слова
FortuneWheelGame.removeSelectedWord()

// Закрытие модали
FortuneWheelGame.closeModal()
```

## Стиль и дизайн

### Консистентность с matches.html
- Единые стили из styles.css
- Tailwind CSS для верстки
- Индиго кнопка для вращения
- Серая кнопка для закрытия
- Красная кнопка для удаления

### Адаптивность
- Центрирующий layout на всех экранах
- Максимальная ширина 896px
- Мобильные: padding 16px
- Десктопные: padding 40px

## Решение проблем

### Проблема: Колесо не видно
**Решение:**
- Проверьте, загружен ли JSON файл
- Откройте консоль браузера (F12)
- Посмотрите сообщения об ошибках

### Проблема: Вращение не работает
**Решение:**
- Убедитесь, что кнопка вращения включена (не серая)
- Кнопка включается после загрузки JSON
- Попробуйте обновить страницу (F5)

### Проблема: JSON не загружается
**Решение:**
- Проверьте формат JSON (используйте jsonlint.com)
- Должен быть массив: `[...]`
- Каждый элемент - строка: `"текст"` или объект: `{"text": "текст"}`

### Проблема: Текст выходит за границы
**Решение:**
- Используйте более короткие слова/фразы
- Максимум ~15 символов на элемент
- Или увеличьте количество сегментов (более 4 слов)

## Примеры использования

### На уроке
1. Подготовьте JSON со словами из темы
2. Загрузите на проекторе
3. Ученики вращают по очереди
4. Удаляют использованные слова

### Для тренировки
1. Создайте список слов
2. Вращайте и устно переводите/объясняйте
3. Удаляйте сложные слова для повтора

### Как развлечение
1. Любой список (города, животные, фильмы)
2. Вращайте и выполняйте задание
3. Пример: выпало "Париж" - расскажи про него
